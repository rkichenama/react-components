import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { SamplePage } from '../shared/storybook.utils';

import Weather from '.';

export const Template = (args) => <Weather {...args} />;

<Meta title='Display/Weather' component={ Weather }
  decorators={[ SamplePage, ]} />

# Weather

<Canvas>
  <Story
    name='Example'
    args={{
      cardCount: 5,
      showDescription: true,
    }}
  >
    {Template.bind({})}
   </Story>
</Canvas>

<ArgsTable story='Example' />