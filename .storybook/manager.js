import { addons } from '@storybook/addons';
import theme from './theme';

addons.setConfig({
  theme: theme,
});

// import { addons } from '@storybook/addons';

// addons.setConfig({
//   theme: yourTheme,
// });